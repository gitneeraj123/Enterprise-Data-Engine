services:
  backend:
    build: ./backend
    container_name: enterprise_backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    restart: always

  frontend:
    build: ./frontend
    container_name: enterprise_frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: always
